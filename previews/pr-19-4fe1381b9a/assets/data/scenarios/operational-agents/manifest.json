{
  "id": "operational-agents",
  "name": "Operational Agents",
  "tagline": "Carry out bounded work with approved tools and evidence of the result.",
  "order": 4,
  "customer_outcome": "A team can control an agent's tool execution, approve exact changes, and recover interrupted work without blindly repeating effects.",
  "maturity": "initial",
  "level": "guided",
  "stage": "customer-build",
  "owner": "RVAS field team",
  "decision_prompts": [
    "Which task is bounded enough to delegate, and when must the agent stop?",
    "Which live facts and tool permissions does the task need?",
    "Where does task state belong, and what must survive a restart?",
    "Who approves an exact change, and how will the system reject stale approval?",
    "How will an operator establish whether an interrupted operation committed?"
  ],
  "lessons": [
    {"id": "task-boundary", "title": "Define the task boundary", "path": "lessons/01-task-boundary.md"},
    {"id": "foundation", "title": "Choose and verify the execution foundation", "path": "lessons/02-foundation.md"},
    {"id": "tool-contracts", "title": "Define the tool contracts", "path": "lessons/03-tool-contracts.md"},
    {"id": "task-state", "title": "Separate context from task state", "path": "lessons/04-task-state.md"},
    {"id": "bounded-execution", "title": "Bound the execution loop", "path": "lessons/05-bounded-execution.md"},
    {"id": "approval", "title": "Approve an exact operation", "path": "lessons/06-approval.md"},
    {"id": "recovery", "title": "Recover without replaying uncertain writes", "path": "lessons/07-recovery.md"},
    {"id": "evaluate-operate", "title": "Prove behavior and prepare to operate", "path": "lessons/08-evaluate-operate.md"}
  ],
  "build_modules": [
    {"id": "task-boundary", "title": "Define scope and refusal", "summary": "Bind a task to explicit record IDs and reject requests outside that scope.", "outcome": "An out-of-scope request fails without changing a record.", "implementation_paths": ["accelerator/sample-data/out-of-scope.json", "accelerator/runtime.py"]},
    {"id": "foundation", "title": "Select offline or live execution", "summary": "Run deterministic local exercises or connect the same engine to a configured Foundry agent.", "outcome": "The selected execution mode produces evidence without silently switching modes.", "implementation_paths": ["accelerator/cli.py", "accelerator/foundry_client.py", "accelerator/main.bicep", "accelerator/scripts/deploy.sh"]},
    {"id": "tool-contracts", "title": "Validate tool requests", "summary": "Define narrow schemas and a synthetic backend with conditional updates and operation IDs.", "outcome": "Invalid arguments and duplicate operation conflicts are rejected.", "implementation_paths": ["accelerator/tools.py", "accelerator/tests/test_runtime.py"]},
    {"id": "task-state", "title": "Persist task progress", "summary": "Keep task progress separate from live records and load it in a fresh process.", "outcome": "Pending approval and prior evidence survive a CLI restart.", "implementation_paths": ["accelerator/state.py", "accelerator/cli.py"]},
    {"id": "bounded-execution", "title": "Enforce execution limits", "summary": "Apply persisted call and time budgets around the shared execution loop.", "outcome": "Exhausted work stops instead of receiving a fresh budget on resume.", "implementation_paths": ["accelerator/runtime.py", "accelerator/validate.py"]},
    {"id": "approval", "title": "Bind approval to a proposal", "summary": "Require a separate operator decision tied to the task, exact arguments, and expected record version.", "outcome": "Only an unchanged approved proposal can reach the write boundary.", "implementation_paths": ["accelerator/runtime.py", "accelerator/tools.py"]},
    {"id": "recovery", "title": "Reconcile interrupted operations", "summary": "Interrupt the process around a tool commit and look up the operation before deciding what happened.", "outcome": "A committed operation is recovered once; an unproven result remains unresolved.", "implementation_paths": ["accelerator/runtime.py", "accelerator/tests/test_runtime.py"]},
    {"id": "evaluate-operate", "title": "Inspect behavioral evidence", "summary": "Run failure cases and distinguish local runtime checks from live model and customer acceptance.", "outcome": "A behavioral report and a named list of remaining pilot requirements.", "implementation_paths": ["accelerator/validate.py", "accelerator/sample-data/expected.json"]}
  ],
  "slides": "slides.md",
  "accelerator": "accelerator/README.md"
}
